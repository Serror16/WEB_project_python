# Обработка HTTP-запросов (POST /report, GET /status, POST /validate).
# Ответственность: только валидация входных данных через Pydantic и вызов соответствующего метода из tax_service.py. Не содержит бизнес-логики.